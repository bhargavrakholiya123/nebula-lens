"use client";
import { ComingSoon } from "../ComingSoon";
export default function SettingsPage() {
  return <ComingSoon section="settings" title="Settings" description="AWS connection, scan schedule, notifications, and account preferences." />;
}
